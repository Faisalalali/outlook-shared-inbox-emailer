{
  "name": "outlook-shared-inbox-emailer",
  "version": "1.0.0",
  "description": "A service that sends emails from a shared inbox using the Outlook API.",
  "main": "backend/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "concurrently \"cd backend && nodemon server.js\" \"cd frontend && npm run dev\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "test_backend": "cd backend && npm test"
  },
  "keywords": [],
  "author": "Faisal Alali",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20"
  }
}
